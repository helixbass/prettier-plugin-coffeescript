(
  for a in b
    c
) if d

(for a in b then c) if d

(a(for b in c then d)) if d

a(
  for b in c
    d
) if e

(x = for a in b then c) if d

(x = for a in b
  c
) if d

(x = a(for b in c then d)) if e

(x = a(
  for b in c
    d
)) if e

(a b, (for c in d then e)) if f

(a b,
  for c in d
    e
) if f

(a b, x = (for c in d then e)) if f

(a b,
  x = for c in d
    e
) if f
