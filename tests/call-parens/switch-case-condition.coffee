switch
  when b c
    d

switch
  when b c then d

switch
  when b(c if e)
    d

switch
  when b(c if e) then d

switch
  when (b(c) if e)
    d

switch
  when (b(c) if e) then d

switch
  when (if b then e)
    d

switch
  when (if b then e) then d
