"use strict"

fn = ->
  "use strict"

f = =>
  'ngInject'
  Object.assign(this, { $log, $uibModal })

f = ->

  'ngInject'

  Object.assign(this, { $log, $uibModal })

f = ->
  'ngInject'


  Object.assign(this, { $log, $uibModal })
